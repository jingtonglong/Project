<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 实心 -->
    <solid android:color="@color/submit_red"/>
    <!-- 边框 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/submit_red"/>
    <!-- 圆角 -->
    <corners android:radius="24dp"/>
    <!-- 边距 -->
    <padding
        android:top="1dp"
        android:bottom="1dp"
        android:left="1dp"
        android:right="1dp"/>

</shape>