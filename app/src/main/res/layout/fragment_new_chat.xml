<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".mvp.view.fragment.NewChatFragment">
    <com.base.sdk.widget.TitleBar
        android:id="@+id/new_chat_title"
        android:layout_width="match_parent"
        android:layout_height="63dp" />

    <!-- TODO: Update blank fragment layout -->
    <android.support.design.widget.TabLayout
        android:id="@+id/tablayout"
        android:layout_width="match_parent"
        android:layout_height="34dp"
        android:background="@color/white"
        app:tabIndicatorColor="@color/text_green"
        app:tabIndicatorHeight="2dp"
        app:tabPaddingStart="@dimen/padding_size_middle"
        app:tabPaddingEnd="@dimen/padding_size_middle"
        app:tabSelectedTextColor="@color/text_black"
        app:tabTextColor="@color/text_black" />


    <android.support.v4.view.ViewPager
        android:id="@+id/tab_viewpager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white" />

</LinearLayout>